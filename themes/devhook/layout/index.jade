if site.posts
    each post in page.posts.sort('date', -1).data
        .post
            .container
                h2: a(href=post.permalink): =post.title

                !=partial('partial/post-info', {post: post})

                .post-notice!=post.excerpt